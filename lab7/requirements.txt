blinker==1.8.2
click==8.1.7
Flask==3.0.3
itsdangerous==2.2.0
Jinja2==3.1.4
MarkupSafe==3.0.2
segno==1.6.1
Werkzeug==3.0.6

# I used a requirements.txt file while running the install of the packages so it added the dependencies here too
# I only did pip install

segno
Flask